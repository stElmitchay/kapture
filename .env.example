# Loggerheads Environment Configuration

# Oracle Keypair Path (REQUIRED for production)
# Generate with: python -m loggerheads.oracle_secure --generate
# NEVER commit the actual oracle-keypair.json file to git!
ORACLE_KEYPAIR_PATH=~/.loggerheads/oracle-keypair.json

# Solana RPC URL (optional, defaults to devnet)
# SOLANA_RPC_URL=https://api.devnet.solana.com

# For testing/demo only (not recommended for production)
# LOGGERHEADS_ALLOW_DEMO_ORACLE=true
